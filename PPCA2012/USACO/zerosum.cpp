#include <cstdio>
#include <string>
#include <algorithm>
using namespace std;
int n;
int main(){
	FILE *in=fopen("zerosum.in","r");
	FILE *out=fopen("zerosum.out","w");
	fscanf(in,"%d",&n);
	return 0;
}